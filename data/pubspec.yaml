name: data
description: data
publish_to: none

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  domain:
    path: ../domain

  locator:
    path: ../locator

  json_annotation: ^4.9.0

dev_dependencies:
  flutter_lints: ^4.0.0
  build_runner: ^2.5.4
  retrofit_generator: ^9.6.0
  json_serializable: ^6.9.5
