name: domain
description: domain
publish_to: none

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  locator:
    path: ../locator

  json_annotation: ^4.9.0

dev_dependencies:
  flutter_lints: ^4.0.0
  json_serializable: ^6.9.5
