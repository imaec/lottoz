name: remote
description: remote
publish_to: none

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  domain:
    path: ../domain

  data:
    path: ../data

  locator:
    path: ../locator

  dio: ^5.8.0+1
  retrofit: ^4.6.0
  logger: ^2.6.0

dev_dependencies:
  flutter_lints: ^4.0.0
  retrofit_generator: ^9.6.0
  json_serializable: ^6.9.5
