name: core
description: core
publish_to: none

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  designsystem:
    path: ../designsystem

dev_dependencies:
  flutter_lints: ^4.0.0
