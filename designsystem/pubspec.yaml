name: designsystem
description: designsystem
publish_to: none

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core

  flutter_svg: ^2.1.0

dev_dependencies:
  flutter_lints: ^4.0.0
